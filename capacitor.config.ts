import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'desafio.cuatro.ionic',
  appName: 'Desafio4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
